#include "main.h"
#include <stdio.h>

/**
 * main - print the name of the file
 * @argc: argument count
 * @argv: passed argument
 * Return: 0 successful
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
