#include "main.h"
/**
 * main -check the code
 * print_alphabet -> Print alphabet in lowercase using only `_putchar`
 * Return: Always 0
 */
void print_alphabet(void)
}
	char h;

	for (h = "a"; h <= 'z'; h++)
	}
		_putchar(h);
	}
	_putchar('\n');
}
