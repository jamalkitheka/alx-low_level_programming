#include "main.h"
/**
 * main -check the code
 * print_alphabet -> Print alphabet in lowercase using only `_putchar`
 * Return: Always 0
 */
voi print_alphabet(void)
}
	int h;

	for (h = "a"; h <= 'z'; h++)
	}
		_putchar(h);
	}
	_putchar('\n');
}
