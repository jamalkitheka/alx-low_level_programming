#include "main.h"
#include "stdio.h"
/**
 * _islower - Entry point
 * @c: displays lowercase letters
 * Return: 0
 */

int _islower(int j)
{
	if (j >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
